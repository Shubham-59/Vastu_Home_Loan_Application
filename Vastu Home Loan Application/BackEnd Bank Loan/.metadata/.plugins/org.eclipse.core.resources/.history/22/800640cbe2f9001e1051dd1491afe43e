package com.vashtu.app.service;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.http.ResponseEntity;
import org.springframework.stereotype.Service;
import org.springframework.web.client.RestTemplate;

import com.vashtu.app.model.Customer;
import com.vashtu.app.repository.CMRepository;

@Service
public class CMServiceImpl implements CMService {
	
	@Autowired
	CMRepository cmrepo;
	
	@Autowired
    private RestTemplate restTemplate;

	@Override
	public List<Customer> getAllLoanApplicationData() {
		String url = "http://localhost:8088/getAllLoanApplicationData";

	    // Use ResponseEntity to get the response as an array of Customer
	    ResponseEntity<Customer[]> responseEntity = restTemplate.getForEntity(url, Customer[].class);

	    // Check if the response is successful (HTTP status code 200)
	    if (responseEntity.getStatusCode() == HttpStatus.OK) {
	        // Extract the array of Customer objects from the response body
	        Customer[] customers = responseEntity.getBody();

	        // Convert array to List
	        List<Customer> customerList = Arrays.asList(customers);

	        // For debugging purposes
	        System.out.println(customerList);

	        return customerList;
	    } else {
	        // Handle the case where the API request fails
	        throw new RuntimeException("Failed to retrieve loan application data");
	    }
	}
	
	

}
